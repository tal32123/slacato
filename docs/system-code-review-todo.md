Send a sub-agent per module to put in a comment on the top of each class stating its responsibilities in a simple way such that a product manager would even understand. You can put at most a paragraph but don't yap too much. Also, am I right? It looks like you rolled out your own authentication. That's fucking weird, no? Shouldn't you use something like passport or something like that, which is natively supported and does authentication on its own so you don't need to do this weird thing. I'm talking specifically about session.ts file, for example and guard.ts. Also is contracts.ts a basically a common practice? I have so many different contracts.ts files In this thing, I would think that the different types should, at the very least, be in a class with a type name or something like that. What's the best practice for Nest.js? I don't know if this is it. I doubt that this is it. Try to see if there's any documentation or best practice or something and give me a link to it. Also in Nest.js is it a good practice that all of the types are in one single class like we have now or not really? I know we could do whatever we want and we don't necessarily want a billion different files. I'm just curious here. Why do we have some of the types with snake case and some with camel case? What the hell is going on here? That doesn't sound like a good practice unless there is a good reason why we're doing this. Are tests a part of CI? Because if they aren't, then they should be. Same as lint and prettier. I think there is a new linter, ask me which one to use. Yeah, I think there's one that's actually supposed to be really fast, ox lint or something like that that does lint end prettier, but let me know. Also, is this a good way to do the listing of evidence? I don't know that it's a good way necessarily. Maybe we should be doing something like role-based access control or something like I don't know there's those things that allow you to basically have different levels of access control such as openfga, So would something like that actually make it a bit better so I don't have to manually add all those where cases and everything inside the list evidence for example in the deals.repository.dot ts Or is that not related here? Also, are we chunking when we embed or anything like that? If not, should we be? I would assume so, but I don't know the type of data that we have right now, how big it is or whatever did you even look at it if not and regardless if so have a team of side sub-agents look into the data we have for the whatever we'd need to embed basically all the tsv files and whatnot. Why are you reading the TSV files directly? That doesn't seem right at all. Shouldn't we on initial run enter all the data into a Postgres database or is that not what it says to do in [Cato_GTM_AI_Engineer_Home_Task.docx](/Users/Tal/Documents/hw-cato/catohw/Cato_GTM_AI_Engineer_Home_Task.docx) ? Also, there is stuff that it is not supposed to be in the relevant services because that makes the code not solid. For example, in deal service, why does it have stuff like number or null that has nothing to do with deal service and same for ISO date or null etc. Some of the stuff is completely not necessary to be in that specific file. why does diagnostics service need to know about the "type" of  provider? Provider should have an interface and all services consuming don't need to know about it due to DI, is this not the case with all of my code where relevant?  decisionAuthority on diagnostic service is just one example of an oddity where i think an RBAC or some sort of access control is  more proper, no? look into this please. why is exportservice directly fetching data and not via repository design pattern? your function names are also quite shit. ex. wtf does this do: referencedEvidence? does the docx even require/request audit logging? exports? Half the features that were built? Can you add a one sentence docstring to each function also? deal-brief.processor.ts also should have split between data fetching and business logic. find all instances of these things i'm just showing examples and fix. also on deal brief processor, function names are horrible. also why is dealbrief processor not SOLID? I will be quite frank, i have no idea wtf is going on here because it is that bad. what if i want to use something else that isn't ollama, why do we not have a design pattern here which will provide via DI the different services based off what the customer chooses, you did some shitty shit why can't we use factory or something???  Also wtf are these long ass lines in many of the files, can't you run a formatter like oxlint or whatever does eslint and prettier together and is fast, idk what its called. 

Oxlint/oxfmt or biome are the ones i was talking about for lint/format btw. 

What would linus torvalds or GoF say about your code?